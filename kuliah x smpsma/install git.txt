update terminal:

export PATH="/usr/local/bin:/usr/bin:/bin:/usr/sbin:/sbin:/Applications/XAMPP/bin:$PATH"

echo $PATH
which uname
which git
uname -a

nano ~/.zshrc
    export PATH=/Applications/XAMPP/bin/
    ganti jd:
    export PATH="/usr/local/bin:/usr/bin:/bin:/usr/sbin:/sbin:/Applications/XAMPP/bin:$PATH"

source ~/.zshrc

github.com/aqajade/htmlcss
windows:
    munculin cmd: windows+r terus ketik cmd , enter 
    ketik git --version
    kalau muncul versinya berarti udh keinstall

macbook:
    munculin terminal: cmd+spasi , ketik terminal , enter
    git --version , butuh command line tools download aja
    brew --version , kalau blm ada install 
        bedanya git sm homebrew: git itu u/ aplikasi coding laptop ke online, homebrew sebagai gerbang install aplikasi macbook
    gh --version , 


di root folder: 
    setting default branch ke main 
        cek list branch repositori
            git branch
            klo g ada isinya buat default branch ke main:
                git checkout -b main

        git config --global init.defaultBranch main
        git branch -m master main
    isi data git 
        git config user.email "emailghantum@gmail.com"
        git config user.name "usernameghantum"
        git config user.email "rezzzarea@gmail.com"
        git config user.name "rezzzarea"
    inisialisasi folder local yg blm tersentuh git
        git init
    mensinkronkan root local ke public repo
        git remote add origin https://github.com/aqajade/htmlcss.git
    cek sinkronisasi
        git remote -v
        klo response ny:
            origin  https://github.com/aqajade/htmlcss.git (fetch)
            origin  https://github.com/aqajade/htmlcss.git (push)
            brrti udh aman

mulai update repo:
    git add .
    git commit -m "updatemsgnybebas"
    git push -u origin main
    
    


############################################################

pastiin branch utamanya main
buat classic token di website

cek direktori: ls
    klo bener ini yg mau di push yaudah gas
git init
git add .
git add --renormalize .

install github cli: cli.github.com
gh auth login
gh repo create apadah --public --source=. --remote=origin --push
gh repo create appreza --public --source=. --remote=origin --push
    klo mau bs --private u/ private repo

cek udh ke sinkron apa blm: git remote -v


semua omongan kan dihisab ya